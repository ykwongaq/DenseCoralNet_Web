# API base URL for the backend.
# In dev mode, the Vite proxy handles /api → localhost:8000, so leave this unset.
# For production (separate domain), set this to your backend URL:
# VITE_API_BASE_URL=https://domainname-api.something.com/api
